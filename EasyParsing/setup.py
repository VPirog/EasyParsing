from setuptools import setup

setup(
    name='EasyParsing',
    version='1',
    packages=[''],
    url='',
    license='',
    author='PiLog',
    author_email='',
    description='Parse Tables and Images from URL',
    install_requires=open('requirements.txt').read().strip().split('\n')
)
